version: "3.9"

services:
  # ── FastAPI Backend ────────────────────────────────────────
  backend:
    build:
      context: ./recomendation_system
      dockerfile: Dockerfile
    container_name: compass-backend
    ports:
      - "8000:8000"
    environment:
      
      - MONGO_URI=${MONGO_URI:-mongodb://localhost:27017}
      - MONGO_DB=${MONGO_DB:-compass}
      - CSV_PATH=/app/data/jobs.csv
    volumes:
      - ./recomendation_system/data:/app/data:ro
      - compass-uploads:/app/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── React Frontend ─────────────────────────────────────────
  frontend:
    build:
      context: ./career-ui
      dockerfile: Dockerfile
    container_name: compass-frontend
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  compass-uploads:
